# Memory index

Per-project memories for **planet-sim** (planet simulator), extracted from the BigSim
program's shared memory on 2026-06-10 when the monorepo was split into standalone repos.
Cross-cutting *program* memories (bigsim-program, parallel-dev, test-execution-policy,
the notebook-discipline notes, the github-repo pointer) stayed in the BigSim archive store;
`[[links]]` to those will dangle here by design.

- [Memory vendored in repo](memory-vendored-in-repo.md) — auto-memory now lives in the repo at `docs/memory/` (committed), junctioned to the hardcoded global Claude path; on a fresh clone recreate the junction or recall misses this history
- [Interactive what-if](interactive-what-if.md) — no-install browser what-if (`docs/interactive/index.html`, precomputed grid) + `explain.py` shared "what changed + why" prose engine + notebook enrichment (panels/sliders/collapsed code), built 2026-06-12; design rationale + the deferred §2/§4/§5 live widgets
- [Planet spin-out roadmap](planet-spinout-roadmap.md) — the editable-ocean GPU project DECIDED + roadmapped 2026-06-12 (NOT built): a separate Julia/ClimaOcean+Makie repo born FROM planet-sim across a contract seam; the TWO-seams split (viz/output velocity-for-display vs forcing/input stress-to-drive-ClimaOcean), planet-sim rungs R1–R3+Rung-C (R1=serialize+producer-agnosticism is what the spinoff binds on), spinoff S1–S5 (exec order S1→S3→S2→S4→decide S5; ECCO=the ocean anchor at S1), + the living-staircase retarget rule; full record plan §11
- [Gas-giant feasibility](gas-giant-feasibility.md) — sketch 2026-06-13 (NOT built): gas-giant atmosphere/vorticity on these engines = 3 tiers (β-plane jets ≈1 rung on `baroclinic_qg.py`; sphere globe = new geometry engine; deep interior = a STEEPER reach NOT out of scope — Busse-annulus QG + rotating Rayleigh–Bénard are reduced entries, only anelastic-deep-shell+dynamo is the wall); the isotropic-condensate≠jets correction; pyqg/Dedalus/EPIC/MagIC; full record `docs/explorations/gas-giant-atmosphere.md`
- [Always push commits](always-push-commits.md) — user wants every commit pushed to the remote immediately, and work committed+pushed at the end of every work batch (push straight to `main`; linear history)
- [No pointer spike lines](no-pointer-spike-lines.md) — standing viz preference (2026-06-12): remove hover spike/crosshair lines (`showspikes=False`, default ON for 3-D scenes) from EVERY Plotly figure now + future; wired in planetmap + eddy_globe, pinned by tests; tooltips/modebar kept
- [Viz prose novice→intermediate](viz-prose-novice-intermediate.md) — standing viz preference (2026-06-12): richer novice→intermediate prose on EVERY figure + the notebook. **`relabel-don't-append` REMOVED by user 2026-06-12 — appended glossaries / term-by-term definitions / hover (`<abbr title>`, verified to survive nbconvert+JupyterLab) are now welcome** (hover stays a cherry atop an always-visible def); the old "NOT a glossary-on-every-plot / skip hover" stance is overturned. Plain-language labels + captions still good; formulas welcome if explained. Wired on the eddy-globe exemplar (pinned by test); **figure sweep DONE 2026-06-12** (planetmap biome+coupler globes, A·B·Δ triptych, Rung-A GIF — each caption-pinned; 6 static Phase PNGs deliberately skipped = notebook-embedded floor; advisor caught: `interactive_map` must sync `annotations` on layer-switch); deep pedagogy stays in the notebook → [[pedagogy-novice-intermediate]]
- [Flatten repo-root gotcha](flatten-repo-root-gotcha.md) — post-flatten `parents[N]` off-by-one (fixed 2026-06-10, 9 spots: tests→parents[2], demos+planetmap→parents[1]); CI's `pip install -e` MASKS the bare-checkout failure, so verify the suite without an editable install
- [Engines are living contracts](engines-living-contracts.md) — freeze-before-reuse DROPPED 2026-06-10 (ADR 0005): extend engines directly + test + changelog, no per-change ADR/re-seal; older "frozen" memories are superseded go-forward (kept: ADR-0001 array boundary + validation triad)
- [Planet rung 1 two-way coupler](planet-rung1-two-way-coupler.md) — rung 1 STARTED 2026-06-10; step-1 passive-tracer engine extension BUILT (∫hθ machine-exact, variance bounded, not monotone); Phase-4 jet IS barotropically unstable (emergent eddies, no imposed wave); **step 2 Phase A BUILT = the feedback MACHINERY (`planet/transport.py`): κ→D bridge `D=C_atm·κ/a²` + band-bulk diffusivity + EBM re-equilibrate; anchor = reduction-to-EBM (structural form-match, rung-0 a fixed point), driven by SYNTHETIC flux so it lands independent of the eddy sim**; forks settled (release-primary + post-saturation life-cycle integral; loop-closes = one pass); **step 2 Phase B BUILT 2026-06-11 = the EMERGENT eddy flux (`planet/eddy_flux.py`): pure-release life-cycle `κ_eff=−∫F̄dt/∫θ̄_y dt` on the unstable jet; instantaneous flux largely REVERSIBLE (irr-frac ~0.1) so the life-cycle integral cancels it; tracer-relaxation-during-release REJECTED (breaks ordering); non-circularity PASSES via the `s₂`/obliquity knob (warming barely moves the channel) — flat<steep state-dependent κ (hold α FIXED); magnitude `κ~10³` ~1000× below rung-0 = named-not-banked (resolution-converged but config-tuned); tight reduction is a FINDING not a match (partial corr ~0.6, near-vacuous → rung 3); GEOMETRY delivered in `transport.py` (spherical operator, P₂-eigenvalue-anchored, cosφ metric order-unity)**; **step 3 BUILT 2026-06-11 = circ-informed precip (`planet/circ_precip.py` + `midlat_center_deg` param on `precip.precip_pattern`) → RUNG 1 COMPLETE: storm-track band centre ← emergent `jet_lat` (default=50° → rung-0 bit-for-bit BY CONSTRUCTION); BANKS seam+reduction+migration-MECHANISM (anchored to jet_lat NOT gradient_peak_lat — gap opens only off-centre); the FINDING = a TRADE not accuracy (model jet ~42°, ~8° equatorward of obs-calibrated 50° → rung-0 stays DEFAULT, circ opt-in); spike#1 grad_peak PINNED ~43–46° (realistic migration mechanism-only) + jet≈grad at present-day; spike#2 KILLED the "rain where flow CONVERGES" anchor (eddy `−∂F̄/∂y` near-vacuous + window-edge artifact = same rung-3 boundary); wet-get-wetter + ITCZ/subtropics still deferred/prescribed)**; next = rung 2 → now SCOPED, see [[planet-rung2-scoped]]
- [Planet rung 2 scoped](planet-rung2-scoped.md) — rung 2 (moist dynamics): **Phase A BUILT 2026-06-11** (`planet/moist.py`, 15 fast tests). Fork = column moist budget NOT fluid-channel (resolved precip PATTERN = rung 3). Advisor **split the deliverable + OVERTURNED a scoped claim**: (1) **RATE unlock, opt-in** = energy-constrained **~2.5%/K LINEAR** factor replacing C–C 7%/K (`precip.py` untouched, stays default); (2) **emergent `P−E` budget, diagnostic** = `(D/c_p)·∂/∂x[(1−x²)∂q/∂x]`, `q=RH·q_sat(T)` — **L CANCELS** (no new `D_q`, reuses rung-1 κ→D bridge), conservative flux form `∫(P−E)=0` machine-exact (area-MEAN not np.trapezoid), **pure diagnostic** (Phase-1 green); **why P−E not full-P** = no honest `E` keeps `P≥0` vs −2.4 m/yr eq export. **OVERTURNED:** budget is **extratropical-ONLY** (eq backwards + subtropics ALSO wrong = `P>E`, hyper-peaked C–C q pushes moisture-flux max to ~20°; only poleward-40° right) — NOT the scoped "subtropics good"; one test pins the mislocation. WALL = prescribed `R_ATM_SLOPE=2 W/m²/K` (cited-closure NOT derived; **not** `B_OLR`). P₂-eigenvalue −6 anchor = it IS the EBM operator. Deferred: ITCZ/Hadley, rung-3 pattern, rung-2.5 MSE-diffusion. Gate **179 passed, 1 skip**. See [[moist-ebm-source]]; full record plan §10. **Rung 2.5 NOW BUILT → [[planet-rung25-mse-diffusion]]**
- [Planet rung 2.5 MSE-diffusion](planet-rung25-mse-diffusion.md) — rung 2.5 (MSE-diffusing moist EBM where **T responds**) **BUILT 2026-06-12** (`planet/moist_ebm.py`, 12 fast tests). Headline = emergent **polar amplification ~1.5× (endpoint) / ~1.4× (area-band)** from moisture transport ALONE (no ice feedback): `D_eff(T)=D_s·(1+β(T))`, `β=(L/c_p)·RH·dq_sat/dT` large-warm/≈0-cold → warming grows tropical `D_eff` → poles warm more; dry EBM warms EXACTLY uniform = null. **Separate model ALONGSIDE rung-0**. **Advisor caught the math**: `D_eff` INSIDE the divergence `∂/∂x[(1−x²)D_eff∂T/∂x]` (cross-term same-order, IS the mechanism; `ebm.py` already places callable `D(x)` inside = rung-1 path). **Design**: ONE nonlinear relaxation, `D_eff` frozen each Strang step **like α(T)**; self-contained (NO `ebm.py` edit); `face="harmonic"` pinned. **Framing (advisor)**: REDISTRIBUTION around a PINNED `⟨δT⟩=ΔA/B` (tight, transport conserves ∫T dx), not added pole warmth; direction BANKED magnitude LOOSE (1.43→1.50 RH 0.6→0.8). **Attribution null (advisor)**: freeze `D_eff` at present → PA=1.0 EXACTLY ⟹ PA is 100% the `dD_eff/dT` feedback, 0% D-shape. **Wall = recalibrate `D_s≈0.30`** (re-derive smaller sensible D to match dry present contrast → no latent double-count; TRADE = single-scalar leaves higher-moment SHAPE residual; target-invariant <5%). **Only D re-opened; `B` FIXED = rung-4 radiation wall; `A`=forcing; uniform ΔA NOT ΔS₀**. Separate from rung-2 `P−E` (subtropical test did NOT flip). Gate **261 passed, 1 skip**. Full record plan §10
- [Planet rung 3 scoped](planet-rung3-scoped.md) — rung 3 (vertical structure → **baroclinic instability**) **PHASE A BUILT 2026-06-12** (linear growth rate; `engines/fluid/layered.py`+`stability.py`, 22 tests, full gate green); scoping below = the Phase-B plan of record. Minimal model = **two-layer free-surface SW (Phillips)**; single layer CAN'T be baroclinically unstable; interface displacement IS the dynamical temperature → heat transport intrinsic (leap past rung-1 passive tracer). **BUILT as a SIBLING engine `LayeredShallowWater`+`LayeredState` (leading layer axis), `ShallowWater`/`SWState` UNTOUCHED → `nl=1` reduction BYTE-IDENTICAL** (advisor: untouched=zero-risk + meaningful cross-engine check vs a silent-ULP-shift refactor; loose single-layer tests). **Periodicity blocker RESOLVED (advisor)**: y-gradient can't be a field on a periodic domain → basic state `(U_k,G_k)` enters as constant BACKGROUND COEFFICIENTS (Doppler `−U∂x`+baroclinic `−G v'`), fields=perturbations, full nonlinear engine **default-off `background`** (=plain nonlinear=bit-for-bit; set=spike linear terms+O(ε²) eddy-eddy=the standard 2-layer turbulence substrate Phase B needs). Coupling=ONLY Montgomery pressure stack. Engine σ matches the 6×6 dispersion matrix **~4% converging** (5.4→4.2→3.9% nx32→128); engine recovers both two-layer Poincaré 0.06%/0.01%. Eady-coeff gotcha: 0.304 vs 0.310 uses LAYER-depth `√(g'H₁)/f₀` NOT equiv-depth. Conservation/mass tests **only** background=None (APE extraction = the signal); no passive tracer (interface IS buoyancy). **First STRUCTURAL `engines/fluid` edit** (triggers full-repo gate) + **first compute wall** (external-mode CFL → growth/Poincaré tests slow). N-layer = within-rung upgrade not a new rung. **Advisor's 3 load-bearing refinements, all spike-answered** (`outputs/rung3_baroclinic_spike.py`, gitignored): (1) **anchor = the two-layer SW eqs' OWN linear stability NOT QG Phillips** — linearize → root the 6×6 dispersion matrix (built from eqs, not a recalled quartic); **first-principles validated** (zero-shear neutral to machine precision `max|Imω|≈3e-20` + recovers BOTH Poincaré dispersions); σ(k) with short-wave cutoff, `λ*≈6.8×L_d≈680km` (baroclinic terms externally anchored: σ_max coeff 0.304 vs EADY 0.310 + literature critical-shear formula; solver is f-plane/Eady-like = NO critical shear, β-critical-shear = Phase-A). QG Phillips = **loose cross-check that CONFIRMS it** (rigid-lid `g→∞` → σ_SW/σ_Phillips→1.004 <0.5% mutual validation; free-surface gap clean ~4%; **caught+fixed the recalled-coefficient trap** = β=0 cutoff `K²=2F`, `F=f₀²/(g'H)`, NOT `2F`). (2) **external-mode CFL = the real cost** (fast barotropic `√(gH_tot)` sets dt while slow baroclinic mode is all that matters): penalty `c_ext/c_int≈14×` but **affordable** (4 e-folds ~3–16s wall) → **GO free-surface**; rigid-lid (barotropic elliptic solve = structural) = named within-rung upgrade; idealized `(g,H,Δρ/ρ)` for modest `√(gH)`+resolvable `L_d` = honest at rung 3 (mechanism not Earth jet speeds). C-grid route reproduces σ within ~4% converging. (3) **A/B split**: **Phase A = LINEAR growth rate** (tight, current energy-conserving engine; N-layer extension, layers couple ONLY via Montgomery pressure, single-layer `tracer=None` bit-for-bit); **Phase B = nonlinear saturated eddy heat flux = THE HEADLINE PAYOFF** — the rung-1 reduction-to-EBM finally **non-vacuous** (`⟨v'·interface'⟩` genuine → `D_eff` near realistic magnitude); needs the **named-but-unbuilt hyperviscosity** + long runs. **Held–Suarez DEFERRED→rung 5** (3-D sphere primitive-eq, over-reaches a 2-layer β-plane). **GO calibration (advisor done-check): GO = BUILDABILITY (high-confidence, textbook-guaranteed); the Phase-B PAYOFF (irreversible flux at realistic magnitude → non-vacuous reduction-to-EBM) is an OPEN BET — untouched by a linear spike, the class downgraded@rung1 / overturned@rung2.** Sources to pin: Phillips 1954/Eady 1949/Charney 1947, Vallis 2017/Cushman-Roisin, Held–Suarez 1994; extends [[shallow-water-source]]. Plan §10
- [Planet rung 3 Phase B — outcropping finding](planet-rung3-phaseB-outcropping.md) — rung 3 Phase B (saturated baroclinic eddy flux = the headline payoff) **SPIKE FINDING 2026-06-13, spike-first returned a NEGATIVE result that re-routes the build**: the free-surface two-layer SW engine **OUTCROPS at saturation** (`h→0` ⟹ `PV=(f+ζ)/h` detonates) — **robust** across `g'∈{0.2,0.8,1,2}`/`H∈{400,500}`/`U_s∈{2,4}`/drag `r∈{0.5…3σ}`. Control = **Froude `U_s/√(g'H)≈η_sat/H`**; the first-saturation **OVERSHOOT** drives peak `η/H≈12·Fr` (≫ the ~4–5× RMS vortex-tail factor — first life cycle dumps mean APE into eddies); stronger drag only **delays** (`r=3σ` still 0.91), stronger strat gives a **bigger** overshoot. Avoiding it needs `Fr≲0.04`⟹`U_s≲1`⟹e-fold ≳370h = **QG cost in the worst tool**. Growth-err 2.2–2.6% confirms it stayed in the validated regime ⟹ **finite-amplitude free-surface WALL**, not a Phase-A regression (Phase A **stays banked**). **Dissipation BUILT + de-risked in the spike** (advisor under-spec CONFIRMED: hyperviscosity alone can't saturate a fixed-`G` infinite-APE reservoir → **bottom Ekman drag** required, Held–Larichev 1996): `DissipativeLayered` = `−ν₄∇⁴` on momentum **and** thickness + bottom drag, **all default-off → Phase-A bit-for-bit**, per-layer mass machine-exact ON; hyper has its **own CFL** `dt≲0.035·dx⁴/ν₄`. **P2 (pre-outcrop finite-amplitude flux) REJECTED as headline** (near-vacuous: unstable mode is down-gradient by definition + `irr~1` trivial w/o sloshing). **Interpretation:** the empirical reason 2-layer turbulence is done in **QG** (Held–Larichev 1996 IS 2-layer QG); free-surface has a hard thickness floor QG lacks → **payoff doesn't live in this engine**. Plan **pre-named** the rigid-lid/QG fork "*if saturated runs make the penalty bite*" → they bit via **outcropping**. **Next = two-layer QG (rigid-lid)**: FFT 2×2 spectral PV inversion (~200 lines, Held–Larichev), no outcrop/no external-CFL, linear anchor = the spike's QG-Phillips cross-check; **new model OUTSIDE `engines/fluid`** (no C-grid reuse / no bit-for-bit reduction = Phase A/B validate **different models**). **QG makes the experiment POSSIBLE, NOT pre-guaranteed** (κ irreversible/well-scaled = still the open bet, finally testable). Pre-registered discriminators carried: **irr O(1)** (rung1 ~0.1) + **`κ/(v'L_d)` O(0.1–10)** (rung1 ~1e-3), validate **dimensionless**. **User picked A: bank + scope QG; the QG build is SCOPED in plan §10 (model/anchor/triad/edges), NOT built this session; home TBD = `planet/baroclinic_qg.py` vs `engines/spectral/`.** [[planet-rung3-scoped]]
- [Planet rung 3 QG built — bet WON](planet-rung3-qg-built.md) — rung 3 Phase B **BUILT 2026-06-13 (`planet/baroclinic_qg.py`, two-layer QG turbulence) and THE OPEN BET IS WON**: saturated baroclinic eddy thickness flux is **down-gradient + irreversible (irr 0.96–1.0 vs rung-1 ~0.1)** at **order-unity dimensionless mixing efficiency `κ/(v'L_d)=0.71–1.27` (vs rung-1 ~1e-3)** → the rung-1 reduction-to-EBM is **finally NON-VACUOUS** (downgraded@rung1/overturned@rung2, won@rung3). Engine = 2×2 spectral PV inversion (`K=0` gauge) + 2/3-dealiased pseudospectral Jacobian + SSP-RK3 + β-plane + hyperviscosity/bottom-drag (default-off); **home settled = lean planet module NOT `engines/spectral/`**. **Advisor's load-bearing catch:** down-gradient + irr~1 are GUARANTEED for any sustained baroclinic state (necessary-not-sufficient = the P2 "irr~1 trivial w/o sloshing" carried) → bet won **only by SHOWING turbulence**: the weak-drag condensate (`v'≈16≫U_s=4`) does, via **inverse-cascade KE spectrum** (84% energy below injection, peak at box scale `0.33k*`, broadband) + irregular EKE(t) + vortex-filament PV snapshot. **Tight leg:** QG dispersion = analytic Phillips to **2e-15** (advisor: bar is 1e-9 NOT 4% — the 4% was free-surface-SW-vs-Phillips, gone here) + `K²=2F` cutoff + **`U_crit=β/F` Charney–Stern** (β re-entry the f-plane SW couldn't test) + SW↔QG rigid-lid bridge **<0.5%** (asserted); nonlinear→linear reduction 0.1%. **Why QG won where SW outcropped:** no thickness floor (never outcrops) + rigid lid filters external gravity wave (no external-CFL). **Banked DIMENSIONLESS+qualitative** — dimensional `κ≈0.7–4e6` landing in Earth's band is COINCIDENTAL+box/drag-dependent; `κ₁=κ₂` is an estimator identity NOT a cross-check; condensate dominates `v'` so `κ/(v'L_d)~1`="mixing-length~L_d-ish". Tests `planet/tests/test_baroclinic_qg.py` (tight+plumbing fast, reduction+flux slow), full gate green. **Rung 3 now COMPLETE (Phase A linear banked + Phase B flux won).** [[planet-rung3-phaseB-outcropping]] [[planet-rung3-scoped]]; plan §10
- [Planet viz animation rungs](planet-viz-animation-rungs.md) — animated eddy-flow viz, 3 rungs A/B/C **DECIDED 2026-06-11**: A=matplotlib two-panel mechanism anim / B=Plotly-globe anim / C=showcase particle globe. **Rung A + the shared frame side-channel BUILT 2026-06-11; Rung B BUILT 2026-06-12 (`planet/eddy_globe.py` — globe anim, no new stack: bounded ~55° true-width NH band, NOT 360°/NOT mirrored; advisor's "builds≠animates" gate → surfacecolor-only frames + per-frame cmin/cmax + redraw=True + structural test; browser play-through user-eyeballed); Rung C **BUILT 2026-06-13** (`planet/flow_globe.py` — generic `FlowField` renderer + `flow_field_from_eddy` + `demo_eddy_particles.py` → `docs/figures/planet-eddy-particles.html` ~758 KB; **three.js r137 UMD vendored inline** `planet/vendor/three.min.js` + repo `NOTICE` full-MIT; band-confined coverage; disclaimer = visible DOM div machine-checked for both clauses + never-hidden; 303 fast-lane pass; browser play-through handed to user) = `three.js`/WebGL **perspective** particle flow-globe (was original Canvas2D; particle technique stays ORIGINAL, three.js the only VENDORED piece → **§6 REVERSES sidestepped→owed**: MIT needs a `NOTICE` file = named deliverable; **vendor three.js INLINE not CDN** to stay offline-self-contained; Canvas2D demotes to a §6-free fallback; **GPU-advection seam shifts DOWN** — CPU BufferGeometry v1 → GPU ping-pong upgrade *within* WebGL, same <~30fps@GCM-res trigger), renderer-agnostic vector-field-on-globe data contract **UNCHANGED** (grid+u,v+scalar+frames+coverage-extent+honesty-label → joins §9.1 registry, scales to GCM/ESM one day) — **the WIN: the amendment touches ONLY the renderer** (contract/§9.3/carve-out/ADR all unchanged = boundary was right); B vs C now both 3-D → **ROLES separate them** (B faithful scalar field/honest-by-construction; C immersive particle showcase/honest-by-disclosure); + honest-by-disclosure carve-out (showcase MAY illustrate beyond model IF a machine-checked on-screen disclaimer documents it; physics-fidelity relaxes, doc verification tightens; A/B stay honest-by-construction, science untouched; ADR 0002 status note).** `n_frames` opt-in `EddyFrames` on `eddy_flux.eddy_life_cycle` (snapshots `(h,u,v,θ)` + cumulative transport traces over full release), **diagnostic-pure** (`n_frames=0` bit-for-bit, `==` asserted); `plots.eddy_life_animation` two-panel (θ-stir+eddy-quiver beside throughput-vs-net budget, fixed colour/quiver scale); `demo_eddy_life.py` banks `docs/figures/planet-eddy-life.gif`. **Advisor caught the design error**: net trace integrates flux PER-LATITUDE FIRST then `Σ|·|` (mean-of-signed leaks SPATIAL cancellation into a TEMPORAL-reversibility curve = overclaim); endpoint ratio lands on `irreversible_fraction` by construction + `window_start` line reconciles full-release vs windowed. §9.4 rule-of-three NOT yet acted on. Honesty edges = channel-not-globe + ~90%-reversible. Plan §9.5
- [EBM radiation source](ebm-radiation-source.md) — Planet P1: cited climlab `EBM` defaults (A210/B2/D0.555/Tf−10/a0 0.30/a2 0.078/ai 0.62/S0 1365.2/s2 −0.48) + benchmark outputs (ice line ~70° observed/North1975, snowball threshold 6-9% Voigt-Marotzke GCM) that ebm.py + climate_reference.py pin
- [Obliquity insolation source](obliquity-insolation-source.md) — Planet §9.1 (built 2026-06-10, the LAST deferred knob now wired): cited daily-mean-insolation formula (Hartmann GPC §2.7 / Berger 1978 / Rose climlab) + mean-annual Legendre s₂(ε) DERIVED (integrate over circular-orbit year, project onto P₂) that obliquity.py uses; ratio-to-Earth recovers climlab s₂=−0.48 @23.44° bit-for-bit (mirrors stellar-albedo-factor); s₂(0)=−5/8 exact, closed form −(5/8)(1−1.5sin²ε), sign-flip ≈54.7°; a₂-fixed scope edge (only insolation responds to tilt)
- [Pedagogy: novice/intermediate](pedagogy-novice-intermediate.md) — planet.ipynb pedagogy roadmap: bucket **C (design-a-world §7 sandbox + `planet_spec.build_spec`/`planetmap.climate_params`) BUILT 2026-06-10**; buckets A (predict-then-check) + B (mission cards) DEFERRED with their concrete prompts/missions
- [Planet interactive-map design](planet-interactive-map-design.md) — Planet's deep-end map design CONVERGED + **ADR 0004 written 2026-06-09**; **planetmap.py v1 + planet_spec.py BUILT 2026-06-09** (biome-map globe + pin-the-schema round-trip; vector_overlay since painted by P4, inert-elevation = the extensibility proof); **§9.1 exoplanet knobs BUILT 2026-06-10** (spectrum→ice-albedo ratio + size→D∝1/size², sliders wired, defaults recover v1 bit-for-bit, two-level-mean honesty; [[stellar-spectrum-ice-albedo-source]]); **§9.1 obliquity knob BUILT 2026-06-10** (the last deferred slider → all 3 knobs spectrum/size/obliquity now wired; [[obliquity-insolation-source]]); **design-a-world §7 bench + two-world diff/compare BUILT 2026-06-10** (`build_spec`/`climate_params` single-source; `planet_spec.diff`=compare-sibling-of-`__eq__` + headless `delta_view` + `planetmap.render_comparison` A·B·Δ webviz triptych reusing `render()`; two batches 18411a8/10559a9): layer registry (Plotly+ipywidgets/`[webviz]`), tier-dependent slider lifecycle (renderer invariant up staircase), planet-spec **pin-the-schema** export/import (JSON+npz / 16-bit-PNG heightmaps / NetCDF-future; round-trip-identity=REAL test), editable-geography seam **preplanned-but-INERT** (north star=regional/orographic precip=rung-5 2-D exit), exoplanet knobs (S₀ amount / spectrum→albedo / size→transport-only); also the **viz/ thin-extraction finding** (3 `plots.py` share conventions not code → NOT promoted, registry=future 3rd consumer)
- [Planet Phase 1 EBM](planet-phase1.md) — latitudinal energy-balance model + Snowball bifurcation: diffusion-spine reuse #3 (sphere heat transport) + Jominy Strang-splitting; the design-fork lesson (the "5.5°C error" was operator-SPLITTING not the engine — engine NOT modified), 3 interchangeable modes (face harmonic/exact × method relax/direct), finite-cap bistability = multiple stable climates at today's sun; [[ebm-radiation-source]]
- [Planet Phase 2 biomes](planet-phase2.md) — climate→biome map: diagnostic precip param + Whittaker (T,P) classifier as an original sloped-boundary TOTAL partition (the Irvin graphical→computation precedent, NOT plotbiomes polygons); cm/yr trap, C-C scales by GLOBAL-T̄; band ordering equator→pole reproduced, warming migrates bands poleward; [[whittaker-biome-source]]/[[precip-parameterization-source]]
- [Planet Phase 3 engine](planet-phase3-engine.md) — `engines/fluid` (2nd shared engine, rotating shallow-water C-grid/SSP-RK3) + circulation.py BUILT 2026-06-09: built nonlinear (finite-amplitude PV seal = the advisor's gate, came out crisp); symmetric scheme conserves **energy not enstrophy** (mass machine-exact only); CFL guard at Courant≈1; tracer slot=seam-not-machinery; Rossby dispersion converges with resolution (not a bug); first multi-engine gate row + import-drift guard live; **Phase 4 coupler BUILT → [[planet-phase4-coupler]]**
- [Planet Phase 4 coupler](planet-phase4-coupler.md) — capstone-completing one-way EBM→shallow-water coupler BUILT 2026-06-09 (emergent jet 16.5 m/s @ 42°): y-periodicity windowed target + flanking-easterly scope edge (don't overclaim 3-band), conservation reframed (forced-exact-mass + release test), emergence proof via synthetic gradient, `vector_overlay` now painted
- [Planet plan](planet-plan.md) — project #3 capstone plan (written 2026-06-09; **ALL 4 phases + map + notebook BUILT = CAPSTONE COMPLETE**, Phase 4 → [[planet-phase4-coupler]]; → [[planet-phase3-engine]]): the 4 locked scope decisions (biomes early / one-way coupler / v1=rung-0 + GCM staircase documented / deep-end interactive map), engine #2 (fluid/shallow-water) now built, Phase-1 EBM reuses Jominy Strang-splitting, precip=parameterization not derived
- [Precip parameterization source](precip-parameterization-source.md) — Planet P2: cited zonal-mean precip-by-latitude structure (ITCZ/subtropics/midlat/poles, cm/yr) + Clausius-Clapeyron **7%/K (moisture-capacity)** vs the **2-3%/K energy-constrained global** rate (named gap); design = pattern(circulation,fixed)×CC(global-T̄) — NOT local-T (over-amplifies); band migration deferred to rung-1/2; **rung-2 update**: the 2–3%/K rate now BUILT opt-in in moist.py → [[moist-ebm-source]]
- [Moist-EBM source](moist-ebm-source.md) — Planet rung-2 Phase A: cited sources `moist.py` pins — Clausius-Clapeyron `q_sat` (Hartmann GPC / Bohren & Albrecht; e₀ 611.2/L_v 2.5e6/R_v 461.5/ε 0.622) + the energy-constrained ~2-3%/K precip rate (**Held&Soden2006/Allen&Ingram2002**; closure `R_ATM_SLOPE=2 W/m²/K` ≠ B_OLR, cited-not-derived) + the fixed-RH diffusive moist EBM (**Flannery1984/Hwang&Frierson2010/Siler-Roe-Armour2018**)
- [Shallow-water source](shallow-water-source.md) — Planet P3: pinned Earth Ω/a/g + β-plane f=2Ωsinφ/β=2Ωcosφ·a, dispersion (Poincaré ω²=f²+gHk², Rossby ω=−βk/(K²+1/L_R²)), L_R=√(gH)/f, Helmholtz adjustment; H=1000m calibration→L_R(45°)≈960km; Gill/Vallis/Sadourny-1975/Arakawa-Lamb refs (energy-OR-enstrophy)
- [Stellar-spectrum ice-albedo source](stellar-spectrum-ice-albedo-source.md) — Planet §9.1: Joshi & Haberle 2012 / Shields 2013-14 host-SED→ice-albedo feedback + Warren 1982 snow optics; two-band a_vis 0.80/a_nir 0.35/λ_c 0.7µm used only as RATIO to solar broadband ≈0.57 (Sun recovers ai=0.62 exactly; M-dwarf ≈0.4); size knob = D∝1/size² DERIVATION (reuses ebm-radiation-source)
- [Whittaker biome source](whittaker-biome-source.md) — Planet P2: cited Whittaker 1975/Ricklefs 2008 Fig 5.5 biome diagram (T °C, P **cm/yr**) + the pinned (T,P) thresholds biomes.py uses; **the GRAPHICAL→original-rule-partition decision = the Irvin precedent** (NOT plotbiomes polygons); cold=T-limited/warm=diagonal-moisture-limited; probes=independent facts (non-circular)
